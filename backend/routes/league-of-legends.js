// Header
const express = require('express'); // Imports Express library
const axios = require('axios'); // Imports Axios library
require('dotendv').config(); // Imports environment variables from .env

const router = express.Router(); // Creates a new "router" (way to group related web routes)

router.get('/PUUID'

)
